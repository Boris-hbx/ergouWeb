# T-174 Retro - W-codex

## Context

- Task: T-174 工作任务表新增「中枢」负责人统筹层
- Branch: `task/T-174-center-attention`
- PR: #1
- Initial submission implemented the schema, `area` / `engage`, zhongshu view, and attention view.

## Rejection Fixes

- Fixed first-entry segment state so `中枢` is highlighted by default instead of `表格`.
- Split zhongshu action area into `decide`, `push`, and scoped `do` sub-sections.
- Scoped `do` visibility to P0 or overdue / due within 2 days.
- Changed risk threshold to overdue / due within 2 days.
- Changed stale track logic to overdue or due within 3 days while `status !== "doing"`.
- Changed attention action count and drill filter to `decide + push`, excluding `do`.
- Updated the zhongshu test checklist for the rejected behaviors.

## Notes

- No new `/api/work/*` endpoints were added in this rejection fix.
- No new LLM tools or tool names were added in this rejection fix.
