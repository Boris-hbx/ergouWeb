# T-297 Retro - W-claude

## Context

- Task: T-297 Praxis·健康板块 v0.3 收口补令（承 T-296 复验的 6 个修正项）
- Branch: `task/T-297-health-followup`（**从 `task/T-296-praxis-health` 切**，stacked PR；T-296 PR#64 尚未并入 dev，Boris 拍板走方案 A）
- 缓存版本 `20260703a → 20260703b`

## 六项修正

- **①[中] M4 派生补 signals 数组**：`derive_from_journals` 原只映射 energy/sleep/moved → 信号维度(掉发/消化/精力)恒「待测」。新增 `signal_to_dimkey` 关键词映射（掉发/脱发→hairloss、消化/肠胃/便秘/腹泻→digestion、精力/疲/累→energy），把 `structured.health.signals[]` 回填对应信号维度打卡；`build_score_input` 对 signal 维度带上最近一条留痕内容，AI 才据以打分。
- **②[中] 维度钻取补 targetGoal + 趋势时间线**：编辑器显示 基线(守)+个人目标(攻) 两类目标（原只显 floor）；新增趋势时间线（习惯/信号=近 21 天打卡点条 绿=done/黄=miss/灰=空；指标=历次实测值序列）；维度设置加 基线/目标 可编辑。
- **③[低] 动态优先级**：`score_health` 打分后对**系统种子维度**每次至多挪一层——做绿(≥80)的核心维移出到常规、恶化(<40)的常规维移进核心；用户自定义维度不动。
- **④[低] addDim 下拉 + 防重复提交**：新增维度改内联表单（类别/优先级/采集 用 `<select>`，替代两次 prompt）；checkIn/recordMetric/deleteMetric/saveDim/createDim 全加 `_busy` 守卫防双击。
- **⑤[低] 八板块 tab 健康分对齐真实 total**：`praxis.js` 加 `setHealthBoardScore`，PraxisHealth 每次 paint 用 `board.total.score` 回填「健康」tab 评分环（原硬编码 58）。
- **⑥[低] derive 不覆盖手动值 / 删光维度不重播种**：derive 改 `ON CONFLICT DO NOTHING`（只补空缺，不覆盖手动/已有打卡）；`ensure_seeded` 计数**含软删行**——种过再删光的用户不再重播种。

## 顺带修 bug

- T-296 遗留：选中维度 → `paint()→loadMetricsIfNeeded()→paint()` 无限重拉循环。改为 `loadDetailIfNeeded` + `_detailFor` 乐观占位守卫，选中只拉一次。

## 验证

- 后端 `cargo test` 全绿 **177(bin)+177(lib)+55(api_tests)、0 失败**（新增 2 项：`signal_keyword_mapping`、`health_no_reseed_after_delete_all`）；`cargo clippy` 0 警告。
- headless Chrome：习惯编辑器（基线+个人目标+近21天时间线 绿/黄/灰点条+streak）、新增维度表单（3 下拉+单位）均正常渲染无 JS 错误；board total=41 回填。

## Notes

- stacked on T-296：PR#65 base=dev，在 T-296 PR#64 合并前 diff 会含 T-296 提交，合并后自动收敛为仅 T-297 增量。
- 无新增 API 端点、无新增 LLM 工具（endpoints.md/tools.json 无需再改）。
- 桌面 only；守动效分级；owner+admin 门禁。
