# 测试用例: 生活模块 & 记账
> 更新日期: 2026-02-24

## 已知 Bug

### ~~BUG-1: 三个核心函数未定义导致模块完全不可用~~ [已修复]

**状态**: 已修复。`currencySymbol()`、`renderCurrencyToggle()`、`loadRates()`、`setCurrency()` 均已在 `expense.js` IIFE 内定义（L1276-L1311），`setCurrency` 已在 return 中暴露。

### BUG-3: expense-amount-prefix 硬编码 "¥" [P2]

**现象**: `index.html` L651 金额输入前缀硬编码为 `¥`，不随币种切换

**修复**: 给 prefix 加 id，在 `setCurrency()` 中动态更新

### BUG-4: 币种切换 HTML 容器不存在 [P3]

**现象**: `#expense-currency-toggle` 在 `index.html` 中不存在

**修复**: 弹窗内添加容器

---

## 测试用例

### 1. 导航与页面切换

| # | 用例 | 前置条件 | 操作步骤 | 预期结果 |
|---|------|---------|---------|---------|
| L-01 | 首次进入生活页 | localStorage 无 life_feature | 点击底栏"生活" | 显示 Hub 页面（记账卡片 + 健康卡片），FAB 不可见 |
| L-02 | 从 Hub 进入记账 | 在生活 Hub 页 | 点击"记账"卡片 | Hub 隐藏，记账列表显示，FAB 可见，数据加载 |
| L-03 | 记住上次打开的功能 | 上次在记账页退出 | 再次点击"生活" | 直接进入记账页（不显示 Hub） |
| L-04 | 从记账返回 Hub | 在记账页 | 点击"← 生活" | 返回 Hub，FAB 隐藏，localStorage 清除 |
| L-05 | 页面切换不干扰 | 在记账页 | 切换到"待办"再切回"生活" | 记账页状态保持，数据正常 |

### 2. 手动记账

| # | 用例 | 前置条件 | 操作步骤 | 预期结果 |
|---|------|---------|---------|---------|
| E-01 | 打开记账弹窗 | 在记账页 | 点击"+" FAB | 弹窗出现，金额输入框聚焦，日期默认今天，备注为空 |
| E-02 | 手动记一笔（最简） | 弹窗已打开 | 输入金额 50 → 点"保存" | 弹窗关闭，列表新增一条记录，日期为今天 |
| E-03 | 手动记一笔（完整） | 弹窗已打开 | 输入金额 25.50 + 日期改为昨天 + 备注"午餐" → 保存 | 记录创建成功，日期和备注正确 |
| E-04 | 金额为 0 不可保存 | 弹窗已打开 | 不输入金额 → 点"保存" | toast "请输入金额" |
| E-05 | 金额为负数 | 弹窗已打开 | 输入 -10 → 保存 | toast "请输入金额" 或阻止提交 |
| E-06 | 取消记账 | 弹窗已打开 | 点"取消"或点击遮罩 | 弹窗关闭，无记录创建 |
| E-07 | 重复记账检测 | 今天已有 50 元记录 | 再记一笔 50 元 | 弹出确认框"发现同日相似记录…确定不是重复记账？" |
| E-08 | 币种切换 — CAD | 弹窗已打开 | 选择 CAD | 前缀显示 "CA$" |
| E-09 | 币种切换 — CNY | 弹窗已打开 | 选择 CNY | 前缀显示 "¥" |

### 3. AI 拍照识别

| # | 用例 | 前置条件 | 操作步骤 | 预期结果 |
|---|------|---------|---------|---------|
| P-01 | 添加照片 | 弹窗已打开 | 点"添加照片" → 选择 1 张图 | 照片缩略图显示，底部按钮变为"识别账单 ✨" |
| P-02 | 添加多张照片 | 弹窗已打开 | 选择 3 张图 | 3 张缩略图显示，可逐个删除 |
| P-03 | 删除照片 | 已添加 2 张照片 | 点击第 1 张的 × | 第 1 张删除，第 2 张保留 |
| P-04 | 超过 10MB 的图片 | 弹窗已打开 | 选择一张 15MB 的图 | toast "图片过大" |
| P-05 | 开始 AI 识别 | 已添加照片 | 点"识别账单 ✨" | 切换到分析状态，显示进度条动画 |
| P-06 | AI 识别成功 | 分析中 | 等待 AI 返回 | 切换到预览状态：显示商家、日期、明细表、金额 |
| P-07 | AI 识别失败 | 分析中 | AI 返回错误 | toast "识别失败，请手动输入"，回到输入状态 |
| P-08 | 预览 → 确认保存 | 预览状态 | 点"确认保存" | 创建记录 + 上传照片 + toast "已记录" |
| P-09 | 预览 → 重新拍照 | 预览状态 | 点"重新拍照" | 回到输入状态，照片清空 |
| P-10 | 预览金额可编辑 | 预览状态（合并模式） | 修改总金额 | 保存时使用修改后的金额 |

### 4. 多日期拆分

| # | 用例 | 前置条件 | 操作步骤 | 预期结果 |
|---|------|---------|---------|---------|
| M-01 | 单日期不显示拆分条 | AI 返回所有 items 同一日期 | 识别完成 | 无拆分提示，正常预览 |
| M-02 | 多日期显示提示 | AI 返回 items 含 2 个不同日期 | 识别完成 | 显示"⚠️ 识别到 2 个日期的账单" + [合并] [拆分] 按钮 |
| M-03 | 选择合并 | 多日期提示已显示 | 点"合并为一条" | 合并按钮高亮，混合展示所有明细，金额和日期可编辑 |
| M-04 | 选择拆分 | 多日期提示已显示 | 点"按日拆分" | 拆分按钮高亮，按日期分组展示，每组有小计 |
| M-05 | 拆分保存 | 拆分模式下 | 点"确认保存" | 创建 N 条记录（每个日期一条），税费按比例分摊 |
| M-06 | 拆分后切换回合并 | 已选拆分 | 点"合并为一条" | 回到合并预览 |
| M-07 | 照片归属 | 拆分保存 | 保存完成后查看各条记录 | 照片仅附在第一条记录上 |

### 5. 列表与筛选

| # | 用例 | 前置条件 | 操作步骤 | 预期结果 |
|---|------|---------|---------|---------|
| F-01 | 按天查看 | 在记账列表 | 选择"天" | 显示今天的记录 + 日汇总 |
| F-02 | 按周查看 | 在记账列表 | 选择"周" | 显示本周记录 + 周汇总 |
| F-03 | 按月查看 | 在记账列表 | 选择"月" | 显示本月记录 + 月汇总 |
| F-04 | 日期前进/后退 | 按天模式 | 点击 ← / → | 切换到前/后一天 |
| F-05 | 标签筛选 | 有带标签的记录 | 点击一个标签 | 只显示含该标签的记录 |
| F-06 | 取消标签筛选 | 已选标签 | 再次点击标签 | 显示全部记录 |
| F-07 | 空列表 | 今天无记录 | 查看 | 显示"暂无记录"空状态 |
| F-08 | 混合币种汇总 | 有 CAD 和 CNY 记录 | 查看汇总 | 显示换算后的 CAD 总额 + 汇率说明 |

### 6. 详情与编辑

| # | 用例 | 前置条件 | 操作步骤 | 预期结果 |
|---|------|---------|---------|---------|
| D-01 | 查看详情 | 列表有记录 | 点击一条记录 | 弹出详情：金额、日期、备注、标签、明细、照片 |
| D-02 | 查看照片大图 | 详情有照片 | 点击缩略图 | 弹出 Lightbox 大图 |
| D-03 | 编辑记录 | 在详情页 | 点"编辑" | 切换到编辑弹窗，字段预填 |
| D-04 | 编辑保存 | 编辑弹窗 | 修改金额和备注 → 保存 | 更新成功，详情刷新 |
| D-05 | 删除记录 | 在详情页 | 点"删除" → 确认 | 记录删除，列表刷新 |
| D-06 | 取消删除 | 确认框弹出 | 点"取消" | 不删除，停留在详情 |

### 7. 边界与异常

| # | 用例 | 前置条件 | 操作步骤 | 预期结果 |
|---|------|---------|---------|---------|
| X-01 | 网络断开时保存 | 离线状态 | 输入金额 → 保存 | toast 显示错误，不丢失输入 |
| X-02 | 极大金额 | 弹窗已打开 | 输入 999999.99 → 保存 | 正常保存 |
| X-03 | 小数精度 | 弹窗已打开 | 输入 10.999 → 保存 | 保存为 10.999 或四舍五入为 11.00 |
| X-04 | XSS 尝试 | 弹窗已打开 | 备注输入 `<script>alert(1)</script>` | 文本被转义，无 XSS |
| X-05 | 快速双击 FAB | 在记账页 | 连续快速点击 "+" 两次 | 只打开一个弹窗 |
| X-06 | AI 识别超时 | 分析状态 | 等待 60 秒 | 超时提示，回到输入状态 |
| X-07 | 空照片提交识别 | 无照片 | 强制调用识别 | 不发请求或提示无照片 |

---

## 修复计划

### 第一步：修复 P0（BUG-1）
1. `expense.js` IIFE 内补全 `currencySymbol()` / `renderCurrencyToggle()` / `loadRates()` / `setCurrency()`
2. `index.html` 弹窗内添加 `#expense-currency-toggle` 容器
3. 导出 `setCurrency`
4. 验证: E-01、E-02、E-08、E-09

### 第二步：修复 P2（BUG-3）
1. 金额前缀改为动态，`setCurrency()` 中同步更新
2. 验证: E-08、E-09

### 第三步：完整回归测试
按 L-01 → X-07 顺序逐条验证
